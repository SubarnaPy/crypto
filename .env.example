# Backend Environment Configuration

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/kryptonics
# Production MongoDB (e.g., MongoDB Atlas):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/kryptonics

# JWT Secret (use a strong random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Client URL (frontend URL for magic links)
CLIENT_URL=http://localhost:8080
# Production:
# CLIENT_URL=https://your-app.netlify.app

# Frontend URL (for CORS - production frontend domain)
FRONTEND_URL=http://localhost:8080
# Production:
# FRONTEND_URL=https://your-app.netlify.app

# ============================================
# EMAIL CONFIGURATION - GMAIL SETUP
# ============================================

# Email Provider: 'resend' (production) | 'gmail' (local) - CONSOLE MODE REMOVED
EMAIL_PROVIDER=gmail

# Gmail Configuration (for local development)
# To enable Gmail email sending:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Generate app password (Select app: Mail, Select device: Other)
# 5. Copy the 16-character password below

EMAIL_USER=tirtho.kyptronix@gmail.com
EMAIL_PASS=your-16-character-app-password-here

# For production deployment, use Resend instead:
# EMAIL_PROVIDER=resend
# RESEND_API_KEY=re_your_api_key_here
# EMAIL_USER=Kryptronix <onboarding@resend.dev>
